{
  "name": "demesne-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript emit target for Demesne — the @demesne/runtime library + worked example.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
