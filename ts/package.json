{
  "name": "demesne-ts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript emit target for Demesne \u2014 the @foir/demesne library + worked example.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "pnpm": {
    "overrides": {
      "vite": "^7.3.5",
      "esbuild": "^0.25.0",
      "postcss": "^8.5.23",
      "nanoid": "^3.3.18"
    }
  }
}
